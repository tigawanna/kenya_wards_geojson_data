export type WardEventTypes = {
  id: string;
  trigger_by: string;
  ward_code: number;
  old_data: string;
  new_data: string;
  sync_status: string;
  sync_attempts: string;
  event_type:string;
};
